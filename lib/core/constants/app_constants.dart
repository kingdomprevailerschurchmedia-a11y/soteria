/// This file contains all the global constants used across the application.
/// It includes application name, version, and other static strings.
class AppConstants {
  /// The name of the application.
  static const String appName = 'Soteria';

  /// The default animation duration for UI transitions.
  static const Duration defaultAnimationDuration = Duration(milliseconds: 300);

  /// Padding values used consistently throughout the app.
  static const double paddingSmall = 8.0;
  static const double paddingMedium = 16.0;
  static const double paddingLarge = 24.0;
}
